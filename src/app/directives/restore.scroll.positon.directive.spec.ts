import { RestoreScrollPositonDirective } from './restore.scroll.positon.directive';

describe('RestoreScrollPositonDirective', () => {
  it('should create an instance', () => {
    const directive = new RestoreScrollPositonDirective();
    expect(directive).toBeTruthy();
  });
});
